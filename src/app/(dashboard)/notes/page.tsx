import { NotesPage } from '@/features/notes/components/NotesPage';

export const metadata = {
  title: 'Notes',
  description: 'Redux CRUD Notes Application',
};

export default function Page() {
  return <NotesPage />;
}
